#!/usr/bin/fish
begin
    xrdb -load ~/.Xresources
    # this is needed to reload GTK settings on X server startup
    lxappearance &
    # wait until it loads
    sleep 0.5
    kill -9 $last_pid
    # kill it

    if test "$BIG_MONITOR" = 1
        set font_size 12
    else
        set font_size 16
    end

end
